# Cloudflare preview deployment record

This is a credential-free record of the explicitly approved Preview
deployment. It intentionally omits the Cloudflare account ID, D1 database ID,
R2 credentials, deployment response bodies, and all Worker Secret values.

- Recorded: 2026-07-21
- Target: `preview`
- Worker: `counterpoint-living-decisions-preview`
- Origin: `https://counterpoint-living-decisions-preview.gs2safari.workers.dev`
- Latest deployed implementation commit:
  `9a3c47fc687cc17187520fe9c56916ffcb072678`
- Latest rendered configuration SHA-256:
  `ad45501da2047c8026513d9fe2e3cf11a8c806bd3c5eb8c186d21d3563d1bfa3`
- D1 binding: database name `counterpoint-preview`; forward-only migration
  check passed through the repository migration set ending at
  `0012_judge_usage_active_request_fingerprints.sql`.
- R2 binding: `counterpoint-artifacts-preview`
- Durable Object bindings: `MEETINGS` (`MeetingCoordinator`) and
  `JUDGE_REALTIME_CALLS` (`JudgeRealtimeCallController`)
- Preview safety flags: `OPENAI_MODE=disabled`, structured/judge Realtime
  routes disabled, and `DEMO_STORY_MODE=enabled` only for the provider-free
  Flagship staged story.
- Verification: security matrix `300/300`, Cloudflare pool `142/142`, target
  dry-run, forward D1 migration check, strict Worker deploy, health/readiness,
  unauthenticated auth smoke, Flagship smoke, and external-host Playwright
  browser checks passed.

The latest deployment also passed the current-tree build and secret scan, and
the provider-free Flagship smoke reported `invalidationEvaluations: 1`. The
Preview judge/Realtime routes remain disabled; no production Secret was read or
registered.

Production has not been deployed or enabled by this record. No production
Secret was registered, and no repository visibility change was made.
