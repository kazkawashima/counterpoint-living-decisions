# Active goal — Descant demo and public-safety boundary

更新日: 2026-07-21
状態: ACTIVE
優先度: submission-critical

## 目的

ハッカソン提出に向け、今回の作業対象を次の3点に限定する。

1. 公開表示名を `Counterpoint` から `Descant` へ変更する。
2. 審査員が迷わず Flagship を再生できる、一本の合成デモ導線を完成させる。
3. リポジトリを公開し、審査用 server-funded AI を本番で有効化する前に、
   秘密情報・個人情報・予算超過・権限越境を防ぐ公開安全境界を証明する。

提出用の表示名は当面 **Descant — Living Decisions** とする。リポジトリ名、
パッケージ名、`@counterpoint/*` の内部 import、D1/R2/Worker の既存リソース名、
DB上の識別子は移行リスクを避けるため変更しない。歴史資料に残る旧称は、現在の
公開プロダクト名と誤認されないよう扱いを分ける。

## 調査で確認した現在地

- Flagship は合成データ、固定デモユーザー、`Context → Permission →
  Commitment → Risk → Review` の5段階、staged event、facilitator resetを持つ。
- OpenAI と judge のCloudflare経路は preview では disabled。したがって現在の
  preview は手動テキスト経路を含む安全な degraded mode として扱う。
- Worker側には judge allowlist、別HMACによるIP擬似化、D1予約台帳、opaque call
  handle、rolling 24時間 USD 25 hard cap、`USAGE_LIMIT_REACHED` の基盤がある。
  start admissionではこの総額だけをロックし、account/IP/meeting/generation/token/
  Realtime秒・同時実行数のrolling二次ロックは解除している。ただしmanaged call
  controllerにはUSD 12予約を守る旧来の30秒・3 response envelopeが残るため、実通話を
  完全なcost-only制御へ移す作業は未完了である。
- providerのUSD 50 alertは二次警告であり、アプリ側のUSD 25制限の代替ではない。
- 2026-07-21、managed Realtime/structured AIの再送・並行開始・meeting越境・
  session/owner差替え・終了競合・二重課金防止の対象スイートを再実行し、Cloudflare
  3ファイルとunit 3ファイル、合計80テストが成功した。これは実装/契約境界の証拠であり、
  hosted C5再実行や実プロバイダーの独立cost実測とは分けて扱う。
- C5の hosted Worker security matrix 再実行、独立したcost-limit証明、3分の人間
  リハーサル、最終公開切替は未完了。production D1/R2/DO、judge Secret、専用
  `judge` identity、exact allowlist付きWorkerは明示承認のもとで有効化済み。
- 直近の hosted parity 修正により、meeting作成、decision history、audit、export
  のWorker経路は実装済み。表示専用tokenの発行・projection・失効もWorkerへ接続済み。
  最新の名称・導線・projector向けshared displayは commit `0d4f0e3` で、
  previewとproductionの両方に明示デプロイ済み。
  旧 `counterpoint-living-decisions.gs2safari.workers.dev` は別Workerで旧称のままなので、
  審査導線には使用しない。
- `docs/思うところつらうら/動作確認後に修正したいこと.md` は検討材料として読むが、
  ユーザー保護のためこのgoal以外の作業で編集・公開しない。
- `221b6d5` preview deployでは security matrix 300/300、Cloudflare pool 142/142、
  health/readiness/unauthenticated auth smoke、Flagship smokeを通過した。実previewの
  read-only probeでも、logged-out root/health/readyは200、認証前APIは401、通常ユーザーの
  judge usageは503 `REALTIME_UNAVAILABLE`、display projectionは200、失効後は401
  `DISPLAY_TOKEN_EXPIRED`、projectionにprivateWorkspace/private textは出ないことを確認した。
- previewではAI経路をdisabledのまま、Flagship meetingに限る provider-free の
  `staged-demo-rule-v1` を明示的な `DEMO_STORY_MODE=enabled` として注入した。これは
  secret、provider call、usage reservationを持たず、production用render configでは強制的に
  disabledになる。したがってpreviewのstaged demoは安全な合成導線であり、production
  judgeを有効化したことを意味しない。
- READMEとjudge credential運用手順は、固定ログイン情報を漏洩しても本番権限にならない
  synthetic local/demo fixtureとして明示し、本番judge credentialのrevoke・rotation・
  Secret delete手順を記録している。ローカル/Preview E2Eはprovider disabled、provider
  failure、usage capのいずれでも手動入力、状態、history/audit/exportが継続することを
  確認している。
- Previewの明示デプロイを、commit、config hash、migration境界、Worker/R2/DO binding、
  safety flags、検証結果を含むcredential-free記録
  `docs/deployments/preview-2026-07-21.md` に残した。productionも同じ形式で
  `docs/deployments/production-2026-07-21.md` に記録し、judge/provider経路を
  productionでoperator-confirmed smokeした。最新UIを含む本番記録は commit
  `0d4f0e38c6a59d546c44ebb5db50f4ab6b004a71` に同期済み。Productionのjudge
  設定は `JUDGE_USER_ID=judge` を含む明示レンダーで復旧・再検証した。
- `221b6d5` preview deployでは security matrix 300/300、Cloudflare pool 142/142、
  health/readiness/auth smoke、Flagship smokeを通過し、実previewのPlaywrightでも
  `AT_RISK → facilitator review → REVIEW_REQUIRED → Action hold/reconsideration task`
  とリロード後の継続、ordinary/judge/displayの別browser context境界を確認した。
- 2026-07-21の外部host Preview Playwright再実行は3/3ケース成功、43.8秒だった。
  これはprovider-free staged review/manual fallback、SPA/API同一origin、
  ordinary/judge/display境界のpath-health証拠であり、初見の人間による3分リハーサルとは
  分けて扱う。
- 一般roomでprivate source未選択のためsharing previewを押せない場合、ボタンの説明、
  `aria-describedby`、native tooltipで解除方法を示すようにした。meeting-creation E2Eと
  synthetic desktop screenshotを更新し、Flagshipの主導線には影響させていない。
- 現行実装commit `16701db` で `npm run security:verify` を再実行し、build、secret scan、
  security matrix `300/300`、Cloudflare pool `142/142` が成功した。これはlocal/contract
  security evidenceであり、production judge/providerやfull hosted C5の代替ではない。
- 2026-07-21の現行treeでも `npm run security:verify` が再成功し、build、secret scan、
  security matrix `300/300`、Cloudflare pool `142/142` を確認した。これは同じく
  local/contract evidenceであり、hosted C5の完了とは扱わない。
- Previewのjudge reconciliation dry-runは `attempted=0 settled=0 released=0 failed=0` で
  完了した。stale rowのSELECTだけを行い、provider、judge/HMAC Secret、D1 mutationを
  発生させていない。これはpre-billable運用経路の確認であり、production judge enableや
  measured derived limitsの完了とは扱わない。
- 2026-07-21、ユーザー裁定として提出プロジェクトのライセンスをApache-2.0に確定し、
  `LICENSE`、root package metadata、first-party synthetic media manifest、NOTICE監査を
  同期した。Apache-2.0のNOTICE・copyright retention・modification notice・trademark
  境界を公開前の運用条件として記録している。
- 2026-07-21、UD-05のdescriptor/tagline/hookをDevpost提出文面として確定し、UD-07の
  審査credentialは非公開Testing Instructions経路で受け渡す裁定を確認した。公開欄・README・
  video・repositoryへcredentialを置かない境界は維持する。
- production Secretを登録しないまま、rollback、judge shutdown、credential rotation/deleteの
  runbookとguarded deployment workflowを確認した。対象shellの`bash -n`とCloudflare
  deployment/config契約22/22が成功し、preview/productionのplan commandもremote
  changesなしで完了した。remote mutationは実行していない。
- 2026-07-21、`docs/思うところつらうら/意見追加.md` の表示密度・projector表示・
  入力/参照境界・オンボーディング・モデル選択の意見を調査し、対応計画を
  `docs/plans/ui-feedback-2026-07-21.md` に記録した。submission-criticalなprojector
  canvas拡張と、直前のFlagship通し手順・3分用ナレーションを
  `docs/presentation/flagship-production-tutorial.md` に反映した。reel制作自体はgoal外のまま。
- 2026-07-21のproduction UI reviewで、Cloudflare Workerに
  `POST /api/v1/decisions/review-resolution` が配線されておらず、revision 3のrecommitが
  503になるP0を確認した。Workerへrecommit / supersede / rejectの3解決を実装し、
  Cloudflare統合14/14とDecision browser E2Eでrevision 3および正しい完了表示を確認した。
  commit `f1d46ed` をcanonical Productionへ明示デプロイし、security matrix 302/302、
  Cloudflare pool 142/142、migration、strict deploy、remote smoke、外部browser境界2/2を
  通過した。認証済みroute probeも400 `VALIDATION_FAILED`を返し、新handlerへの到達を
  確認した。実judge状態でのrevision 3確定だけをhosted最終確認として残す。
- 同reviewで、managed Realtimeの非retryableな`USAGE_LIMIT_REACHED`を自動再試行し、
  後続のidempotency conflictで原因を隠すP0を確認した。browser API errorの
  `retryable`/detailsを保持し、非retryableなcap応答は1回でtext fallbackへ移り、
  reservationと元の理由を表示するよう修正した。generation上限は回帰fixtureとして
  保持するがproductionでは非適用で、現行のusage lockはUSD 25 cost capだけである。
  unit 14/14とbrowser E2E 1/1が成功した。

## 2026-07-22 Flagship reliability slice

- 設計書のruntime修正、全Flagship操作inventory、alternate branch E2E、desktop / mobile /
  reduced-motion証拠を実装した。最終local検証はbrowser `30/30`、unit/integration
  `901/901`、Cloudflare `140/140`、typecheck、lint、build、secret scan、media manifest、
  format、diff checkが成功した。
- 6つの変更状態は合成データのみで18枚を保存し、Apache-2.0のfirst-party provenanceと
  hashをmanifestへ記録した。Cloudflare 1102時のRetryボタンはcontrastを修正し、
  computed-style assertionと画像目視の両方で確認した。
- final tree `2616150` は明示承認済みのguarded workflowでcanonical Productionへ
  deploy済みで、security matrix `335/335`、Cloudflare `140/140`、migration、strict
  deploy、remote smoke、provider-free Flagship smokeが成功した。このsliceに残るのは
  private/shared judge `Connect → Connected → Disconnect` のowner観測だけであり、synthetic
  E2Eから完了推定しない。hosted C5、独立cost実測、3分人間リハーサル、public切替、tag、
  submissionは引き続き別gateとして未完了に保つ。

## 2026-07-22 Production Realtime recovery slice

- judge managed Realtimeの失敗を、provider本文やkeyを出さない5種類のsafe reasonへ閉じ、
  UIがcredential/account拒否、offer不正、Location/SDP不正、transport障害ごとの回復方法を
  表示できるようにした。失敗後retryとmanual text継続をbrowser E2Eで固定した。
- ordinary userのBYOK configure / heartbeat / access / client secret / clearをCloudflare Workerへ
  配線し、meeting coordinatorのmemory-only短命leaseとlogout時失効を実装した。Node serverに
  routeがあるだけではWorker parityの証拠にしない。
- 実OpenAIへのNode media-only smokeは現在のkey、multipart WebRTC request、
  `gpt-realtime-2.1`で成功した。local workerdはjudge routeまで到達するが、このPCの必須proxyを
  Worker `fetch()`へ継承できず、HTTP statusなしの`PROVIDER_UNAVAILABLE`で停止する。この環境差を
  provider実装の不具合と誤認してconnectorを変更しない。
- secret-safe workerd smoke、Worker実route E2E、safe recovery screenshotを追加済み。残るrelease
  gateのうち、全local deterministic verification、production deploy、100% served version監査、
  ordinary BYOK private/shared接続・Disconnect・clearのhosted確認は完了した。Nodeでは見えなかった
  native `fetch` receiver差もRED testで再現し、client-secret / managed call / sidebandを共通wrapperへ
  修正した。残るのはprivate credentialを持つjudgeによるserver-funded private/shared接続確認だけである。

## 2026-07-22 Judge ephemeral Realtime recovery slice

- provider call受理後のDurable Object sidebandだけが本番で失敗し続け、BYOKのbrowser direct
  WebRTCはordinary/judgeの双方で成功したため、提出用judge UIを短命client-secret経路へ統一した。
  Workerはallowlist認可後に`OPENAI_API_KEY_JUDGE`を30秒のchannel-scoped credentialへ交換し、
  standard key自体はbrowser、D1、R2、DO、ログ、responseへ出さない。
- no-keyのprivate/shared `Connect → Connected → Disconnect`、optional judge BYOK、managed
  `/realtime/calls` request 0件をbrowser E2Eで固定した。usage summaryはhiddenとし、direct
  RealtimeへD1 USD 25 hard capが効くとは主張しない。structured judge AIの既存reservationは維持する。
- commit `a10c4e1`をmainへpushし、canonical Production Worker version 29へdeploy済み。security
  `337/337`、Cloudflare `149/149`、remote smoke、active binding監査が成功した。さらにownerが
  personal BYOKなしのjudgeでPrivate agentとShared room agentの双方の接続成功を確認した。
  Judge ephemeral Realtime recovery sliceは完了とする。

## 完了条件 A — 名称変更

- [x] ユーザーに見える正式表示を `Descant — Living Decisions` に統一する。
- [x] 少なくとも README の見出し・説明・デモ説明、HTML title/meta、Web UIの
      Brand、footer、ARIA label、エラー文、JSON download名を棚卸しして更新する。
- [x] Devpost、README、画面内の製品主張が同じ名称と同じ主張境界になる。
- [x] `Counterpoint` が残る箇所を「公開表示」「提出文書」「歴史資料」「内部識別子」
      に分類し、公開表示・提出文書には意図しない旧称を残さない。
- [x] リポジトリ名・内部namespaceを変更しないことをREADMEまたは開発メモで明示し、
      不要なDB migrationやimport全面変更を発生させない。
- [x] 命名変更後に typecheck、lint、build、および既存の該当E2Eを通す。

## 完了条件 B — 一本の審査デモ導線

### 審査員の開始点

- [x] ログアウト状態の公開URLから、最初に表示される画面だけで「これは合成データの
      staged hackathon demo」であること、次に押す場所、AIが使えない場合の手動経路が
      分かる。
- [x] 固定の合成ユーザーでログイン後、room作成・参加者割り当てを要求せず、seed済み
      Work & Productivity Flagship を一覧から開ける。
- [x] judge credentialは公開README、動画、リポジトリ、Devpostの公開欄へ置かず、
      非公開と確認できた審査用Testing Instructions等の経路だけで渡す。

### 導線の価値弧

- [x] facilitator一人（必要なら別タブの合成participant）で、次の順序を画面上で追える。
      `Private context → exact disclosure preview → owner approval → shared Evidence
      → human Decision commit → staged external event → AT_RISK → human review →
      REVIEW_REQUIRED / Action hold / reconsideration task`。
- [x] 各段階に一つの主操作と短い説明があり、初見でも次の操作を推測できる。
- [x] `Private`、`Shared`、`AI inferred`、`Human confirmed`、`Staged demo story` を
      混同しない表示にする。staged eventは実世界の連続監視・実在規制通知ではないと
      明示する。
- [x] `Commit` 後に history、audit、exportを取得しても、未接続経路を artifact storage
      エラーとして誤表示しない。API失敗時は対象機能、原因、再試行または手動継続を示す。
- [x] OpenAI disabled、provider障害、judge cap到達のいずれでも、既存のroom状態、
      手動excerpt、Decision編集、history、JSON exportを保持する。
- [x] Flagship resetは対象meetingだけを初期状態へ戻し、再実行して同じ価値弧を再生できる。
- [ ] `REVIEW_REQUIRED`からhuman resolutionを実行し、recommitではrevision 3をappendして
      history/audit/exportへ反映する。ローカルWorker・browser証拠とcanonical Production
      deploy、route到達確認は完了し、実judge状態でのhosted recommit確認だけを残す。
      操作と期待結果は`docs/verification/production-reviewer-walkthrough.md`に固定し、
      ユーザー指定により実確認をレビュワーへ委任する。
- [ ] 初見の通し実演を約3分以内に収める。facilitatorとsynthetic participantが、
      micまたは同じdurable endpointのmanual text、private-agentのstaged cue、exact
      excerpt承認、human Decision commit、staged event、AT_RISK→REVIEW_REQUIRED、
      history/audit/exportを一本の導線で再生する。実時間は人間リハーサルで計測する。
      ユーザーが後ほど実施するため、現時点では未完了のまま保持する。

### UI検証と記録

- [x] UIを変更した各sliceで、対応するPlaywright browser E2Eを追加・更新し、実行結果を
      commitする。
- [x] `localhost`だけでなく、Cloudflare previewまたはTailscale相当の外部hostで、SPA、
      same-origin API、artifact、必要な realtime URLにCORS/localhost固定の失敗がない。
- [x] desktop、mobile、reduced-motionの主要状態を合成データだけでスクリーンショット
      に残す。reel制作自体はこのgoalの完了条件に含めない。

## 完了条件 C — 公開安全境界

### リポジトリ・配布物

- [x] `.env`、`.dev.vars`、API key、Cloudflare token、D1/R2 dump、実ユーザー情報、
      private source、ログ、未確認の動画・音声・画像をtracked file、README、screenshots、
      submission packageに含めない。
- [x] `.env.example` は空のplaceholderと安全な説明だけを持ち、judge keyは記載しない。
      `OPENAI_API_KEY_JUDGE` と `JUDGE_IP_HMAC_SECRET` は別々のCloudflare Worker Secret
      とし、source、vars、D1、R2、DO、browser、ログへ複製しない。
- [x] 固定デモパスワードは実アカウントの認証情報ではなく合成demo-only credential
      と明示し、seed dataもすべて架空にする。公開後の漏洩を前提に、judge credentialは
      失効・rotateできる運用を確認する。
- [x] 直接依存・推移依存・同梱mediaのlicense/NOTICEを棚卸しし、公開切替前に
      `MIT` または `Apache-2.0` 等の最終方針を決める。未確定のまま公開しない。

### 認証・権限・AI費用

- [x] previewでは `OPENAI_MODE=disabled`、judge structured/realtime route disabledを
      維持し、未設定・障害時は fail-closed しながら手動経路を残す。
- [x] productionでjudge modeを有効にする場合、exact allowlistのjudge user、正規化済み
      `CF-Connecting-IP`、別HMAC secret、全feature flag、production bindingを同時に
      満たさない限り利用できない。
- [x] judgeはデフォルトでserver-fundedでBYOK不要。allowlist済みjudgeだけは望む場合に
      自分のキーをこのタブへ入力でき、明示的なHTTPS requestでWorkerが短命client secret
      発行にだけ使う。普通のユーザーはjudge keyを選択・継承・利用できず、server-funded
      standard keyやprovider call IDはbrowser response/logへ出ない。短命client secretだけは
      認可済みbrowserへ返し、private/shared direct WebRTCに利用する。
- [ ] rolling 24時間 USD 25だけをアプリ側hard capとして強制し、USD 50 provider alertは
      二次警告として扱う。account/IP pseudonym、meeting、concurrency、Realtime seconds、
      generation、tokensの二次ロックはjudgeでは適用しない。
      上限到達時は新規server-funded provider requestを出さず、明示的な
      `USAGE_LIMIT_REACHED` と手動継続を返す。start admissionの二次ロック解除は完了したが、
      controller内の30秒・3 response envelopeを、USD 12予約残高に基づくcost-metered
      terminationへ置き換える作業が残る。提出用のbrowser-direct judge RealtimeはこのD1
      ledgerを通らないため、sideband修復と独立実測が終わるまでhard-cap達成には数えない。
- [x] managed realtime/structured AIの各routeについて、同一リクエストの再送、並行開始、
      meeting越境、session/owner差替え、終了競合をテストし、二重課金しない。
- [x] managed Realtime startが非retryableなusage limitを返した場合は自動再試行せず、
      元のlimit理由と予約量を表示して、meeting stateと手動text経路を維持する。

### 公開前の証明

- [ ] security matrixをhosted Worker経路でも再実行し、IDOR、meeting isolation、
      private disclosure、SSRF、upload spoofing、expiry/revocation、webhook replay、
      secret/log leakageを確認する。ユーザー指定によりこのsliceは後回しとする。
- [x] logged-out browserでroot/health/ready/auth boundaryを確認し、private dataや
      judge-only情報が認証前に見えない。
- [x] ordinary user、judge user、display tokenを別browser contextで試し、権限境界と
      public displayのprojectionが仕様どおりである。
- [x] preview/productionの対象commit、config hash、migration状態、Worker/R2/D1/DO
      resourceをcredential-freeなdeployment recordへ残す。自動deployではなく、明示承認
      したコマンドから実行する。
- [x] 本番secretを登録する前に、rollback、judge shutdown、credential rotation/deleteを
      実行可能な手順として確認する。

## 実施順（時間制限下の最小計画）

1. **名称の棚卸し** — 公開面と内部識別子を分類し、Descantへ変更する対象を確定する。
2. **デモ導線の収束** — seed済みFlagshipを主導線に固定し、画面文言、主操作、manual
   fallback、失敗表示を整える。
3. **公開安全境界の検証** — secret scan、hosted security、judge/ordinary/displayの
   境界、$25 cap、degraded modeを通す。
4. **提出面の同期** — README、英語topics、Devpost用説明、license/NOTICE、testing
   instructionsを同じ名称・同じdemo storyへ揃える。
5. **最終ゲート** — previewでリハーサル後、production deployとpublic visibility switchを
   分離して承認する。production judge secret登録と公開化は、A〜Cの未完了項目がゼロに
   なってから行う。

## 今回の非目標

- reelの撮影・編集・投稿そのもの
- Google OAuth、一般公開の自己登録、通常ユーザー向けの課金
- voice UXの全面改修、テーマ切替、汎用オンボーディング、Quickstart/Meta demo
- リポジトリ名や内部namespaceの移行
- 承認なしにproductionへ先回りしてデプロイすること

## 完了の定義

このgoalは、名称変更後のFlagshipを外部hostのclean browserで再生でき、AIが
disabledでも手動経路で価値弧が完走し、hosted security/secret/cost/authorizationの
証拠が揃い、README・提出説明・testing instructions・license方針が一致した時点で
ACHIEVEDとする。スクリーンショットやreel素材の記録は開発規律として継続するが、
reel完成はこのgoalの達成条件から外す。
